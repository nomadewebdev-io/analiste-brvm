[build]
  base    = "web"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirige toutes les routes vers index.html (SPA)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
